package com.example.javacore;

public class SynchronizationExample {

	/*
	 * The synchronized keyword is used to synchronize a method or block of code.
	 * Synchronization is the process of making sure that only one thread can access
	 * a shared resource at a time.
	 *
	 * It keeps all concurrent threads in execution synchronized.
	 */
}
