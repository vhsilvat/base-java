package com.example.arraytypes;

import java.util.ArrayList;

public class ArrayListExample {

	/*
	 * The ArrayList class implements the List interface.
	 * By default, the ArrayList class is not synchronized, which means that
	 * it can be accessed by multiple threads simultaneously.
	 *
	 * ArrayLists increase its size by 50% when it is full.
	 *
	 * The iterator interface is used to iterate over the elements of an ArrayList.
	 */

	ArrayList<String> list = new ArrayList<>();
}
