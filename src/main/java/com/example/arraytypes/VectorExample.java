package com.example.arraytypes;

import java.util.Vector;

public class VectorExample {

	/*
	 * The Vector class implements the List interface.
	 * Vectors are synchronized, which means that they can be accessed
	 * by only one thread at a time.
	 *
	 * Vectors increase its size by 100% when it is full.
	 *
	 * Both the iterator and enumerator interfaces are used to iterate
	 * over the elements of a Vector.
	 */

	Vector<String> list = new Vector<String>();
}
