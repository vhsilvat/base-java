package com.example.spring;

public class DispatcherServletContextLoader {

	/*
	 * DispatcherServlet is the front controller of Spring MVC. It is responsible
	 * for loading the spring bean configuration and dispatching the request to
	 * the appropriate handler.
	 */

	/*
	 * ContextLoaderListener is a listener that is responsible for starting and
	 * stopping the WebApplicationContext in Spring root.
	 */
}
