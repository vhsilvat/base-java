package com.example.spring;

public class DispatcherServletContextLoader {

	/* Ambos são componentes cruciais para o aplicativo spring, onde DispatchServlet
	lida com solicitações web e o ContextLoaderListener cuida da configuração geral
	do aplicativo. */

	/*
	 * DispatcherServlet é parte central do spring MVC, gerencia o ciclo de vida de
	 * requisições HTTP e controladores no contexto da web.
	 */

	/*
	 * ContextLoaderListener cria e gerencia o contexto raiz do spring, carregando
	 * informações como configurações globais e beans compartilhados.
	 */
}
